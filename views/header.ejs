<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
      #btn{
        border: none;
        background-color: transparent;
        margin: 0px;
        opacity: 0.9;
        padding: 0px;
      }
      #btn_contact{
        border: none;
        background-color: transparent;
        margin: 0px;
        opacity: 0.9;
        padding: 0px;
        margin-right: 50px;
      }
    </style>
</head>
<body>
    <header class="navbar">
        <div class="nav-container">
          <div class="logo">
            <a href="/listings">
              <h2>WanderLust</h2>
            </a>
          </div>
          <nav class="main-nav">
            <ul>
              <li><a href="/listings">Home</a></li>
              <li><a href="/listings/new">Add New Listing</a></li>
              <li><button onclick="window.scrollTo(0,5000)" id="btn">About</a></li>
              <li><button  onclick="window.scrollTo(0,5000)" id="btn_contact">Contact</a></li>
            </ul>
          </nav>
          <div class="auth-buttons">
            <%if(isuservalid === undefined) { %>
              <a href="/login" class="btn btn-outline">Login</a>
              <a href="/signup" class="btn btn-primary">Sign Up</a>
            <% } %>
            <% if(isuservalid !== undefined) { %>
              <a href="/logout" class="btn btn-primary">Logout</a>
            <% } %>
          </div>
        </div>
      </header>  
</body>
</html>